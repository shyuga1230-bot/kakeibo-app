# 💰 かけいぼ

大学生のためのシンプルな家計管理アプリです。  
ログイン不要・外部API不使用・データはブラウザ内に保存されます。

---

## 概要

| 項目 | 内容 |
|---|---|
| アプリ名 | かけいぼ |
| 対象 | 大学生 |
| データ保存 | この端末のブラウザ内（IndexedDB） |
| ログイン | 不要 |
| 外部API | なし |
| バージョン | 0.1.0 |

### 主な機能

- 支出の記録・編集・削除（連続入力モード対応）
- カテゴリ別の円グラフ
- カレンダー表示
- 月別予算管理
- 固定費の管理（有効／無効の切り替え）
- 電卓
- カテゴリのカスタマイズ（色ピッカー）
- データのバックアップ・復元（JSON形式）

---

## データについて

> **重要：** データはこの端末のブラウザ内（IndexedDB）にのみ保存されます。  
> サーバーへの送信は一切行いません。  
> 機種変更・ブラウザデータの削除・アプリの削除をするとデータが消える場合があります。  
> **定期的に「設定 → バックアップを保存」からJSONファイルとしてバックアップしてください。**

---

## PWA としてホーム画面に追加する方法

ホーム画面に追加すると、アプリのように起動できます。

### iPhone（Safari）

1. Safariでアプリを開く
2. 画面下部の「共有」ボタン（□↑）をタップ
3. 「ホーム画面に追加」を選ぶ
4. 名前を確認して「追加」をタップ

> **注意：** iPhoneではSafariのみ対応しています。ChromeやFirefoxからは追加できません。

### Android（Chrome）

1. Chromeでアプリを開く
2. 右上の「⋮（メニュー）」をタップ
3. 「ホーム画面に追加」または「アプリをインストール」を選ぶ
4. 「追加」をタップ

> バナーが自動で表示される場合はそこからでも追加できます。

---

## 技術スタック

- **Next.js 16** (App Router)
- **TypeScript**
- **Tailwind CSS v4**
- **Dexie.js v4** (IndexedDB)
- **Recharts v3**
- **date-fns v4**
- **lucide-react**

---

## 開発

```bash
npm install
npm run dev          # 開発サーバー起動
npm run build        # ビルド
node scripts/generate-icons.js  # PWAアイコンを再生成
```

---

## ライセンス

MIT
